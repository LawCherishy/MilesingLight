GODOT BEHAVIOR CONCEPT SPACE REBUILT AS ORIGINAL MILESING C++20

STATUS
This is a concept-derived feature map, not a release checklist.  It neither
copies Godot source nor claims that every mapped spatial/editor feature is
present in v0.2.  REALIZATION.txt and executable verification identify the live
subset; the remaining lines designate original Milesing C++ work to be built.

READING RULE
Each line states the useful behavior learned from the supplied Godot source, then the original C++/Light and Microsoft-native realization. Godot code, engine, scene model, formats, assets, editor structure, and dependencies remain reference material. Named Light functions execute inside the feature on real typed operands; they are never decorative labels. Divinand remains with the complete authored work.

LIVING APPLICATION
1. Recursive authored formation — one graph can carry conceptual bodies, relations among relations, exact Light expressions, 2D/3D placement, media, time, portals, and further worlds. CORE: `term`, `group`, `compose`, `operad_substitute`, and shared authored routes.
2. Same work in 2D and 3D — SELFIE `diagonal(work)` can supply the same authored work to paired portrayal coordinates where that exact map is intended; ordinary shared references keep edits unified.
3. Native application shell — wWinMain, Win32 window class/message loop, DirectX renderer, local recovery opening, and machine acquisition dormant.
4. Workspaces — MY LIGHT, 2D FRACTAL, 3D FRACTAL, OPERATIONS, TIME/MEDIA, and LIGHT TREATMENT are immediate portrayals of one app body.
5. Docked/floating tools — navigator, inspector, resources, timeline, diagnostics, command palette, and recovery surface with custom C++ docking geometry and Win32 child/secondary windows where useful.
6. Open-work tabs — local works remain reachable and explicitly reorderable through SHIFTY, CEREMONIES, or FUSING when those operations are chosen.
7. Navigator — searchable authored graph and immediate-level portrayal; HIERARCHY `covers` derives supplied poset covers and COMMUNITY `concept_close` derives shared-attribute closure.
8. Inspector — typed geometry, Light operands, expression, purpose, portrayal, media, time, resource, and recovery editors.
9. Command palette — actual Light callable names and every editor command search through one semantic registry; COMMANDSEA orders a selected action before compatible work.
10. Local browser/transfer — application-associated local paths, private fragment transfer, deliberate system clipboard export, and OLE local drop with bounded inspection.
11. Progress/cancellation — std::jthread/stop_token, explicit work allowances, partial purposeful results, residual expression, and clean release.
12. DPI/resizing — per-monitor Win32 DPI, DXGI resize, DirectWrite scaling; CHANGELY can operate on a supplied resize sequence while mul/div perform actual scale arithmetic.

2D AUTHORED WORLD
13. Infinite-feeling 2D — camera pan/zoom, clips, cached geometry, requested detail, and exact authored coordinates. `add/sub/mul/div`, `compose`, WHYS `preimage`, and FILTERAGE operate where their domains fit.
14. Attention — click, box, lasso, hierarchy, text, relation, plural attention, and keyboard traversal. `preimage`, CHEST `convex_hull`, EMPTISM, SIZE, and PERMISE apply to real supplied fields/predicates.
15. Direct transforms — move, rotate, scale, resize, pivot, exact entry, preview, commit/cancel. GIVENS rotation plus central arithmetic run inside the C++ transform functions.
16. Grid/rulers/guides/snap — visible measures and nearest-purpose placement; MEDITATIONS `prox` may resolve a supplied snap objective.
17. Relation editing — endpoints, ports, paths, curves, waypoints, styles, labels, converse, and removal. TENSIONS calculates oriented differences; QUEST routes supplied graphs.
18. Curves/paths/polygons — Direct2D path geometry plus actual convex hull, log spiral, Voronoi, face map, and cyclic shift calls.
19. Images/tiled formation — WIC images, crops, masks, repeated cells, texture atlases; RELIEF erosion and FILTERAGE act on their real supplied structures.
20. Text/math — DirectWrite editable Unicode and structured executable Light expressions powered by the same registry used for evaluation.
21. Ordering/grouping — group, release, visibility, locking, shift, mix, reverse, nested entry, and same-work portals.
22. 2D camera travel — bookmarks, frame, smooth travel, bounds. PRESENTLY samples a supplied camera function; CHANGELY measures discrete movement.
23. Generated 2D — SPIRALLY, EARTH CITY, FRACTALLY, CREATIVITY, CHAOSISM, and GROWING produce actual requested geometry/rules under finite bounds.
24. Fields/particles/material portrayal — bounded generated instances, blends, textures, and HLSL inputs from actual expressions.
25. 2D routes/safe regions — QUEST `astar_path`, CHEST `convex_hull`, and SAFETY `safe_gfp` on supplied graphs/regions.

3D AUTHORED WORLD
26. Spatial world — conceptual formations, map relations, exact expressions, media, geometry, and embedded editors in navigable space. D3D11/DXGI/DirectXMath with authored values retained above graphics floats.
27. Cameras — perspective/orthographic, orbit, pan, fly, dolly, frame, focus, bookmarks, limits. GIVENS supplies plane rotations; METHODICALLY composes compatible maps.
28. Split views — several cameras portray the same authored body through multiple D3D11_VIEWPORT regions.
29. Ray/region attention — unproject pointer, bounded ray/AABB/triangle or ID-buffer query; WHYS `preimage` describes supplied projection-to-hit source retrieval.
30. Transform gizmos — local/world axes, planes, pivots, move/rotate/scale, numeric entry. DirectXMath plus GIVENS and central arithmetic.
31. Spatial grid/snap — lines, origin, rulers, nearest geometry, supplied proximal objective, and connection tensions.
32. Generated meshes — actual Hutchinson fractals, L-system steps, logarithmic spirals, Voronoi regions, convex hulls, simplex faces, Givens rotations, growth, and cyclic shifts become vertices/indices/normals.
33. Instancing — shared geometry with distinct authored placements; GROWFLY/GROWING generate finite placements and FUSING/SHIFTY alter supplied orders.
34. Detail/reveal — frustum/BVH and requested scale combine with FILTERAGE, SAFETY, and SIZE to choose the current finite manifestation.
35. Materials/shaders — editable typed parameters, textures, blend/depth/rasterizer choices, embedded HLSL, bounded generated fields, and visible shader supply.
36. Lighting/environment — directional/point/spot, fog, backgrounds, shadows, readable depth; actual arithmetic controls intensity and placement.
37. 2D surfaces in 3D — complete Direct2D/DirectWrite editor renders to a DXGI target used as an editable 3D portal texture.
38. 3D surfaces in 2D — spatial camera output renders into a planar portal with exact authored route and return.
39. Paths/navigation — QUEST least-cost routes, authored splines, focus travel, and literal SPIRALLY travel when selected.
40. Collision/spatial bounds — AABB/OBB/sphere/triangle/BVH plus real convex hull, existence predicate, and safe-region calculations.
41. Deformation opening — joints, skinning, constraints, Givens plane rotations, and composed transforms for later authored beings/media.
42. Spatial audio — local decoded sources/listener positions, LOUDA decibel power conversion, movement difference, WASAPI output.
43. Spatial diagnostics — wireframe, normals, overdraw, bounds, selected IDs, resources, draw counts, and timings.

PORTALS AND RENDER PLANNING
44. Offscreen portrayal — 2D editor, 3D world, camera, media frame, or generated result into a bounded D3D texture.
45. Recursive portals — 2D-in-3D and 3D-in-2D through cycle-aware route traversal, depth/texture ceilings, grouped requests, bounded growth, and visible return.
46. Render dependency timing — TIMELY `maxplus_mv` computes supplied pass completion times; ALLAGE folds completed pass receipts under an associative combine.
47. Purpose-sized detail — FILTERAGE, SAFETY, SIZE, finite traversal, frustum/BVH, mip/resource selection.
48. Quality/cost choice — measured candidates may pass through BEST `pareto_front`; the person or authored purpose selects among nondominated results.
49. Device recreation — authored work survives DXGI resize/device removal; dependency-closed resources regenerate from recipes.

INPUT, LANGUAGE, TIME, AND MEDIA
50. Mouse/pen/touch — WM_POINTER samples feed the same attention, drawing, transform, and camera commands.
51. Keyboard/text — Win32 keys + TSF/IME + DirectWrite; canonical Light words parse directly to callable C++ entries; NAMA handles binding-safe rename.
52. Gesture-sized history — each human intention carries preview, commit/cancel, inverse/reapply, and receipt; mathematical reverse remains authored-order reversal.
53. Mathematical portrayal — structured term/grouping, readable operation/operand order, exact text, glyph layout, and executable source.
54. Search/completion — commands, Light words, authored bodies, properties, resources, and routes; preimage can trace portrayed results to supplied sources.
55. Timeline tracks — properties, transforms, expressions, audio, video, markers, nested sequences. PRESENTLY, MEMORY, TIMELY, and CHANGELY execute their exact supplied domains.
56. Keyframes/curves — add/move/copy/scale keys, interpolation, angles, Bézier portrayal, finite sampling, and discrete difference.
57. Playback/scrub/loop — clock, seek, ranges, markers; PRESENTLY, SHIFTY, CEREMONIES, and PROMAGE act when their actual meanings are requested.
58. Sequence alignment — TRACKA `dtw_path` aligns supplied finite traces under an explicit memory bound.
59. Audio analysis — PSYCHE `dft`, LOUDA `decibel`, MEDIA `median3`, TRACKA, waveform/frequency portrayal.
60. Local images — WIC bounded decode/encode, metadata preview, exact authored transforms, textures, generated image targets.
61. Local SVG — Direct2D SVG portrayal plus original supported-element translation when editability is requested.
62. Audio/video playback — Media Foundation source reader, D3D textures, WASAPI buffers, authored time, bounded decode, seek, release.
63. Generated media output — requested finite images/frames/audio results written locally through WIC/Media Foundation only after preview.

RESOURCES, SIGNALS, DIAGNOSTICS, ACCESS, AND PERMANENCE
64. Typed resources — images, meshes, materials, fonts, media, expressions, and embedded works share original generational handles and RAII.
65. Shared source/local variation — shared body plus deliberate authored variation; SELFIE diagonal may supply the same operand into paired positions.
66. Change signals — typed dependency notification wakes only affected portrayals; COMMANDSEA orders change before dependent work and TIMELY schedules dependencies.
67. Dependency closure — ALLAGE, ORGANIZATIONS, and SAFETY operate on supplied member/partition/reachability bodies; original graph code remains associated with loading.
68. Background local loading — bounded reads, stop tokens, partial portrayals, main-thread GPU upload, no ambient disk scanning.
69. Import opening — original bounded local parsers for images, media, fonts, geometry, and future formats; reference implementations never enter product code.
70. Extension opening — PLUGINS `operad_substitute` inserts typed recipes into declared compatible openings; active native extension loading remains a later deliberate capability.
71. Performance view — app-caused CPU/GPU/frame/resource counts, CHANGELY difference, SIZE, ALLAGE, optional PSYCHE periodic analysis; live machine values require the exact grant path.
72. Render inspection — alternate local shaders/portrayals for wireframe, normals, bounds, IDs, overdraw, and resource cost.
73. Bounded operation log — actual command, exact Light function, operands, purpose, result, resource use, and divinand; no continuous ambient system history.
74. Accessible interface — native UIA fragments/patterns over the same commands, expression structure, formations, relations, timeline, inspector, portals, recovery, and focus graph.
75. High contrast/reduced motion — Windows preferences change portrayal and time sampling while the authored work remains whole.
76. Native writing — canonical chunked Milesing body, SHA-256, candidate, flush, protected prior, ReplaceFileW, reopen, recovery.
77. Migration — ordered C++ transformations into a new candidate; Light exact names/places and authored relations compare before/after.
78. Offline package — MSVC C++20 `/MT`, declared Windows imports, embedded shaders/font/assets, local manifest, no server/account/telemetry/updater/network route.

REFERENCE AREAS UNDERSTOOD
Godot editor workspaces/docks/inspector/command palette; 2D and 3D editors; cameras/viewports; scene/resource composition; animation/timelines; material/shader/media behavior; change signals; diagnostics/profiling; Windows input/windowing; accessibility; writing/recovery/package behavior.

MICROSOFT DOCUMENTATION
https://learn.microsoft.com/en-us/windows/win32/direct3d11/atoc-dx-graphics-direct3d-11
https://learn.microsoft.com/en-us/windows/win32/Direct2D/direct2d-and-direct3d-interoperation-overview
https://learn.microsoft.com/en-us/windows/win32/api/directxmath/nf-directxmath-xmvector3unproject
https://learn.microsoft.com/en-us/windows/win32/medfound/processing-media-data-with-the-source-reader
https://learn.microsoft.com/en-us/windows/win32/coreaudio/wasapi
https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-interfaces
