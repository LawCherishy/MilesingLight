# Public realization status

Status date: 2026-07-14  
Release: `v0.2.0-alpha`

The public source, documentation, and hardened Windows x64 artifacts are verified for v0.2. The annotated tag `v0.2.0-alpha` designates the release, and the release commit is that tag's target.

## Implemented and verified

- One x64 native C++20 Win32/DirectX desktop application assembled from seven implementation bodies plus one generated binding-data include.
- Exactly 112 self numbers: the original 111 authored routes plus outer LIGHT TREATMENT associated with REALITY.
- Exact authored binding data for all 112 entries and all ten fields per entry: 1,120/1,120 fields.
- A finite executor and typed fixture for every current operation route: 112/112 pass independently of the authored Direct proof fields.
- Six shared workspaces: **MY LIGHT**, **2D FRACTAL**, **3D FRACTAL**, **OPERATIONS**, **TIME / MEDIA**, and **LIGHT TREATMENT**.
- A read-only Operations binding sheet opened with `B`, with bounded scrolling and self-number navigation; its interaction suite verifies that inspection does not mutate drafts, invoke operations, or alter receipts.
- Authored invocations retain operand values, roles, spatial positions, expression routes, exact results, consumer, and purpose.
- Explicit LIGHT TREATMENT windows measure exact finite portions of the authored world, current MilesingLight process and renderer, admitted local machine, and explicitly active local workspace file.
- Measured bytes become exact bit wholes with source, duration, position, typed mathematical views, and graph ancestry.
- Recursive SIZE and DESIGNING derivation followed by finite sheaf gluing produces one exact LIGHT TREATMENT global whole when every overlap agrees.
- The completed global relation and a separately measured unary `converse` route construct a live MY LIGHT `DivinandWhole` retaining exact source, route, duration, result, recursive ancestry, expression, receipt, and rooted spatial associations.
- Completed whole results can enter later compatible operations, giving the retained operation body infinitely many lawful usable finite openings under repeated derivation while each hardware run remains a measured finite cut.
- Canonical version-2 writing, parsing, replay-derived results, SHA-256 verification, protected prior, bounded recovery inventory, and exact authored-world/attention restoration.
- LIGHT TREATMENT and MY LIGHT expressions, operand designations, results, and receipts survive normal v2 save/reopen. Ephemeral raw occurrences and complete in-memory carrier objects remain outside the serialized body.
- Strict published-v1 migration validates all 111 historical node self-number/operator/origin tuples. Each of the eight changed-arity routes validates its exact historical result before deterministic receipt enrichment and current replay; every unchanged route requires result presence and canonical equality with current replay. Accepted work associates LIGHT TREATMENT with REALITY and reserializes as stable canonical v2; malformed or altered routes are rejected.
- Open and Save As admission independently validates directly attached local-volume roots, and every save revalidates its target immediately before target inspection and writing. UNC, extended/device, unknown-root, remote-volume, and any existing reparse-point component are held. A missing suffix is allowed only after every existing parent proves direct and local. This pathname validation is not handle-relative/no-follow race elimination, so residual pathname TOCTOU remains.
- Launch and workspace opening remain measurement-dormant. Measurement begins through `Q`, `D`, or `Enter` in LIGHT TREATMENT and can be completed or revoked explicitly.

## Nine constructive verification suites

```text
PASS 112/112 finite executor verifications
PASS 112/112 exact authored bindings (1,120 fields)
PASS read-only Operations binding-sheet interactions
PASS live whole-self derivation: measured bits + duration + recursive SIZE/DESIGNING + LIGHT TREATMENT sheaf glue + recursively reusable MY LIGHT DivinandWhole(converse); acquisition=<ticks> ticks, adaptive period=<ticks> ticks
PASS authored LIGHT TREATMENT + MY LIGHT association: exact expressions + operands + receipts survive normal canonical workspace persistence
PASS canonical codec: every Operand tag + 112 applied self-number fixtures round-trip byte-exactly
PASS strict version-1 migration: 8 changed arities + exact receipt enrichment + current replay + stable v2 + 18 altered-result/arity/presence rejections
PASS recoverable local writing: first + replacement + protected prior + SHA-256 + exact reopen
PASS canonical realization: attention + exact Editor restoration + replay-derived results
```

The final public-tree build ran these suites together with unified MSVC C++20 `/W4` compilation and native-code inspection. The GUI build completed with zero `/W4` warnings.

## Whole-self expansion evidence

One real **60.005809-second** run produced:

| Axis | Exact result |
|---|---|
| Lossless generative retention | `G=776` retained canonical bytes; `E(4096)=706,050` separately expanded canonical bytes; `E/G=909.858×`; replay verified |
| Exact selected measured-bit ancestry | `S=4,445` bytes; `B=35,560=8×S` bits; factor `1.000×` |
| Temporal observation retention | `A=16,618` acquired bytes; `R=2,391` latest-family retained bytes; `A/R=6.950×` |

The evidence-based **60-minute projection**, using the observed recurring payload, cadence, one-time contribution, and the same bounds, is `A=875,900`, `R=2,391`, `A/R=366.332×`. It is explicitly a projection. Temporal coalescing releases intermediate external observations; the lossless generative axis applies to deterministic consequences of retained exact operands and routes.

The structural family has infinitely many lawful usable openings through repeated finite derivation. The reported `909.858×` is the measured finite `n=4096` cut, not a finite numerical substitute for that structural infinity.

## Privacy boundary

The authored source adds no server, account, telemetry, updater, cloud service, socket, HTTP client, or public data collection. Measurement is confined to the authored work, MilesingLight's own process/window/renderer, explicitly admitted local-machine facts, and the explicitly active local file. Live raw retention is bounded and released after completion or revocation.

The final DLL import boundary is exactly `USER32.dll`, `ole32.dll`, `bcrypt.dll`, `d3d11.dll`, `dxgi.dll`, `d2d1.dll`, `DWrite.dll`, and `KERNEL32.dll`. `WS2_32`, `WINHTTP`, `WININET`, and `URLMON` are absent.

## Final release evidence

Two independent hardened GUI builds from the public tree were byte-identical.

```text
Annotated tag: v0.2.0-alpha
Release commit: annotated-tag target
Compiler: MSVC 19.51.36248
Toolset: 14.51.36231
Windows SDK: 10.0.26100.0
Warnings: zero at /W4
File: MilesingLight.exe
Size: 1,504,256 bytes
SHA-256: C3C4AE2A85BBD812DD69A1067C0F7B5D4859723E3FE8EA2E6F6465A87A591F1E
Independent builds: byte-identical
Architecture: x64 PE32+
Subsystem: Windows GUI
Mitigations: high-entropy VA; dynamic base/ASLR; NX/DEP; CFG instrumented with FID table; CET compatible; /GS
Manifest: embedded; requestedExecutionLevel=asInvoker; uiAccess=false
Reproducibility: reproducible-build debug record present
Certificate directory: empty
Authenticode: NotSigned; SignTool reports no signature
Imports: USER32.dll, ole32.dll, bcrypt.dll, d3d11.dll, dxgi.dll, d2d1.dll, DWrite.dll, KERNEL32.dll
Forbidden import check: WS2_32, WINHTTP, WININET, URLMON absent
Self-test file: MilesingLightSelfTest.exe
Self-test size: 1,510,400 bytes
Self-test SHA-256: 9B1A533120ED74830DB0D8295B13FC31AD49312A01FF6190A0D8714D3F0B2DEB
```

The executable and self-test remain untracked build artifacts. The source-first Git tree carries the source, exact binding data, documentation, licenses, and checksums; the GUI executable is distributed separately as the tagged release asset.

## Deliberate next capacity

- complete create, delete, rename, connect, disconnect, resource, and property authoring;
- broader 2D/3D portrayal of recursively derived graph wholes and their routes;
- media import and WIC-facing authoring controls;
- complete accessibility-provider behavior;
- canonical persistence for workspace selection, cameras, timeline/playback, undo history, portrayal caches, uninvoked operation drafts, and any additional live whole-carrier detail deliberately admitted to the file format.

These are published directions rather than present-tense release claims.
